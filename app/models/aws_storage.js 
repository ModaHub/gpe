// aws_storage.js
'use strict';

var Bookshelf = require('./bookshelf');

var aws_storage = Bookshelf.Model.extend({
  tableName: 'aws_storage'
});

module.exports = Bookshelf.model('aws_storage', aws_storage);
